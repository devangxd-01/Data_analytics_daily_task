### 🚀 15-Day Progress Log

**Week 1: The Core Basics**  
Started from a blank sheet creating student records. Quickly moved past manual calculations 
using `SUM`, `AVERAGE`, and percentage formulas. Cleaned up the layout with `Ctrl + T` (Tables) 
and set up basic pass/fail logic using `COUNT`, `COUNTA`, and `COUNTIF`.

**Week 2: Data Cleaning & Text Wrangling**  
Experimented with `TODAY`, `NOW`, and `RANDBETWEEN`, then used Conditional Formatting to spot trends at a glance. 
Spent a few days fixing noisy data—standardizing casing with `PROPER`/`UPPER`, stripping extra spaces with `TRIM`, 
and removing symbols with `SUBSTITUTE`. Ended the week merging and splitting full names using `TEXTJOIN`, `LEFT`, `RIGHT`,
and `FIND`.

**Week 3: Dashboards & First Look at SQL**  
Built a dynamic view using Pivot Tables, interactive Slicers, and charts, bringing everything together into a clean student
performance dashboard. On the final day, stepped outside Excel to write basic SQL queries (`CREATE`, `INSERT`, `SELECT`) 
to see how spreadsheets translate into relational databases.
